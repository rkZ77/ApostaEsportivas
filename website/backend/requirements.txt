fastapi==0.136.3
uvicorn[standard]==0.49.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
psycopg2-binary==2.9.12
python-dotenv==1.2.2
python-multipart==0.0.32
requests==2.34.2
anthropic==0.108.0
openai==2.52.0
httpx==0.28.1
mercadopago==3.2.0
email-validator==2.3.0
pydantic==2.13.4
resend==2.10.0
rapidfuzz==3.14.5
# Reduz o escudo servido por /api/proxy ao tamanho que a tela usa. A origem
# manda 150x150 com 20 a 45KB pra desenhar a 16-32px · a Home baixava 370KB so'
# de escudo. Uso restrito a main.py::_reduzir_logo, que devolve o original se
# a biblioteca falhar.
#
# 12.3.0 e' a versao em que a reducao foi testada, e tem wheel
# cp312-manylinux_2_28_x86_64 -- mesma plataforma dos outros pacotes do build,
# entao instala binario e nao compila nada.
#
# O pin anterior (11.4.0) QUEBROU O BUILD: aquela versao nunca existiu. Pinar
# sem conferir o indice custou um deploy.
Pillow==12.3.0
