# Sonda temporaria (12/09/2026).
#
# O CI nao executa um unico passo desde que nasceu: os tres jobs morrem em 2
# segundos, sem runner. Este arquivo existe pra separar duas explicacoes que a
# API publica nao separa · se ele TAMBEM morrer, o problema e da conta ou do
# repositorio no GitHub, e nenhuma mudanca no ci.yml resolve. Sai do repo
# assim que a resposta chegar.
name: Ping

on:
  push:
    branches: [dev]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "o runner comecou"
