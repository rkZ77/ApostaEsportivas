# Dependencias do pipeline (ApostaEsportivas/src), pra rodar na maquina local.
#
# Em producao este arquivo NAO e' instalado: o Dockerfile copia src/ pra
# /app/pipeline e o pipeline roda como subprocesso dentro do container do
# backend, usando o que website/backend/requirements.txt instalou. Se entrar
# dependencia nova aqui, ela precisa entrar la tambem.
#
# A lista anterior (httpx, pandas, numpy, scipy, playwright) nao tinha um
# unico import no codigo -- era heranca de uma fase antiga do projeto.

anthropic
openai         # so services/league_analysis_service.py (comando "ligas")
psycopg2-binary
python-dotenv
requests

# Mesmo motivo do requirements do site: `zoneinfo` le o banco de fusos do
# sistema, o Windows nao tem nenhum, e os coletores e os dois pipelines ao vivo
# chamam ZoneInfo("America/Sao_Paulo") pra decidir o dia de hoje em Brasilia.
# Sem isto o motor nao roda num Python limpo fora do Linux.
tzdata
